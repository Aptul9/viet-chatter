node_modules/
dist/
build/

# Runtime data
logs/
*.log

# SQLite
*.db
*.db-journal
*.db-shm
*.db-wal

# WhatsApp Web session
.wwebjs_auth/
.wwebjs_cache/

# Transformers cache (embedding model ~80MB)
.cache/

# Env
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Drizzle generated meta (keep migrations sql, ignore journal)
drizzle/meta/_journal.json

# tsx / build artifacts
*.tsbuildinfo

# Next.js build output
.next/

# User-edited YAML config (example is committed, real one is local)
config/user-config.yaml
config/user-config.yaml.backup

# e2e (Spec B): runtime artefacts. Code under e2e/{driver,validator,config}/src
# IS committed, but per-run data and the second WhatsApp session are not.
e2e/driver/.wwebjs_auth/
e2e/driver/.wwebjs_cache/
e2e/logs/
e2e/db/
